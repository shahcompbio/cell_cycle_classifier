============
Installation
============

At the command line::

    $ pip install cell_cycle_classifier

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cell_cycle_classifier
    $ pip install cell_cycle_classifier
